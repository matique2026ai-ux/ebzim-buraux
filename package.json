{
  "name": "ebzim-monorepo",
  "private": true,
  "scripts": {
    "build": "cd backend && npm install && npm run build",
    "start": "cd backend && npm run start:prod"
  },
  "dependencies": {
    "axios": "^1.15.2",
    "mongoose": "^9.4.1",
    "puppeteer-core": "^24.42.0"
  }
}
